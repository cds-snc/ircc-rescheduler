// Verify Items and functions on the calendar page. 
describe('Calendar page functions', () => {
    beforeEach(() => {
      cy.visit('http://localhost:3004/calendar')
    })
  
    it('should be able to select days', () => {
      cy.visit('http://localhost:3004/calendar')
    //  cy.url().should('contain', '/calendar')

    }
)});