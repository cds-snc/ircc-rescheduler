#!/bin/sh

# COMMIT_HASH=`cat /web/VERSION`
# SENTRY_RELEASE=$RAZZLE_STAGE-$COMMIT_HASH SENTRY_AUTH_TOKEN=$SENTRY_AUTH_TOKEN  yarn sentry_map

# this file is not currently being used. the entry point is added directly to Dockerfile for simplicity.
yarn start